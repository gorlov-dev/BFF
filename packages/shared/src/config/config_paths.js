// -------------------------------------------------------- root

const root = "/";

// --------------------------------------------------------

const pkgs = root + "/packages";
const publicDir = root + "/public/src";

const path = {
    // --------------------------------------------------------

    root: root + "/",
    pkgs,

    // -------------------------------------------------------- log

    log: root + "/log/src/",

    // -------------------------------------------------------- Public

    public: publicDir,
};

export default path;
